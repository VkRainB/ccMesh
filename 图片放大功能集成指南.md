# VitePress 图片点击放大功能集成指南

> 本文记录 ccMesh 文档站「图片点击放大」功能踩过的坑与正确做法。
> 下次重建 VitePress 文档、需要集成图片放大/缩放时，按本文操作即可一次到位。

---

## 一、最终结论（TL;DR）

用 `vitepress-plugin-image-viewer`（底层 viewerjs）做图片放大时，**必须显式关闭它的 CSS 过渡**：

```ts
// docs/.vitepress/theme/index.ts
imageViewer(route, '.vp-doc', { transition: false })
```

否则在本站这种环境下会出现：**点击图片后只有灰色遮罩、没有图片、也无法关闭**。

---

## 二、问题现象

- 点击正文图片后，弹出一层灰色遮罩；
- 遮罩里**看不到图片**；
- 点关闭按钮 / 点遮罩 / 按 ESC **都关不掉**，只能刷新页面；
- 本地 `localhost:5173`、线上 `github.io` 都会出现；
- 控制台**没有任何 JS 报错**，正文里的图片本身也都加载正常（`naturalWidth` 正常）。

## 三、根本原因

`vitepress-plugin-image-viewer` 默认使用 viewerjs 的 `transition: true`。在这个模式下，viewerjs 的「显示」和「关闭」**依赖 CSS 过渡结束事件 `transitionend` 作为回调**：

- 显示：先加遮罩(opacity 0) → 淡入(opacity 1) → 等 `transitionend` 触发后，才把图片真正放进 `.viewer-canvas` 并完成显示；
- 关闭：先淡出 → 等 `transitionend` 触发后，才把遮罩 DOM 卸载/隐藏。

而在本站环境下，这个 `transitionend` **不会自动触发**（典型的过渡卡死 / 标签页渲染节流问题）。结果 viewer 永远停在「中间态」：

- 遮罩在（灰色），但 `opacity` 卡在 `0`；
- `.viewer-canvas` 里**没有 `<img>`**（图片没被放进去）；
- 关闭流程也因为等不到 `transitionend` 而无法完成 → 关不掉。

> 一个很有迷惑性的现象：只要触发一次**强制重排（reflow）**（例如 DevTools 里读一下布局、窗口 resize、或截图），viewer 就会「突然」恢复成正常显示。这正是「依赖 transitionend 卡死」的典型特征 —— 不要被它骗了，以为是偶发问题。

## 四、排查方法（可复用）

用 Playwright 直接在页面里跑 JS 做「同步检查」，是定位这类问题的关键：

```js
// 点击图片后，同步立刻检查 viewer 状态（不要 setTimeout / 不要读多余布局，避免重排掩盖 bug）
() => {
  document.querySelector('.vp-doc img').click()
  const c = document.querySelector('.viewer-container')
  const img = document.querySelector('.viewer-canvas img')
  return {
    opacity: c ? getComputedStyle(c).opacity : 'none',
    classes: c && c.className,                       // 看是否还带 viewer-transition
    canvasImg: img ? img.naturalWidth + 'x' + img.naturalHeight : 'EMPTY',
  }
}
```

判断标准：

| 结果 | 含义 |
|---|---|
| `opacity:0` + `canvasImg:EMPTY` + class 带 `viewer-transition` | **坏的**：卡在过渡中间态 |
| `opacity:1` + `canvasImg:903x484` + class 不带 `viewer-transition` | **好的**：同步显示成功 |

注意：`getComputedStyle` 本身会触发重排，可能「治好」bug 从而掩盖问题。所以要在**点击后同一段同步代码里立即取值**，才能拿到真实状态。

## 五、从零正确集成（标准步骤）

### 1. 安装依赖

```bash
npm i viewerjs vitepress-plugin-image-viewer
```

### 2. 在主题入口里启用

```ts
// docs/.vitepress/theme/index.ts
import DefaultTheme from 'vitepress/theme'
import type { EnhanceAppContext } from 'vitepress'
import { useRoute } from 'vitepress'
import imageViewer from 'vitepress-plugin-image-viewer'
import vImageViewer from 'vitepress-plugin-image-viewer/lib/vImageViewer.vue'
import 'viewerjs/dist/viewer.min.css'

export default {
  extends: DefaultTheme,
  enhanceApp(ctx: EnhanceAppContext) {
    ctx.app.component('vImageViewer', vImageViewer)
  },
  setup() {
    const route = useRoute()
    // 关键：transition:false —— 让 viewerjs 同步显示/关闭，不依赖 transitionend，规避灰屏/无图/关不掉
    imageViewer(route, '.vp-doc', { transition: false })
  },
}
```

### 3. 在 Markdown 里正常写图片即可

```md
![ccMesh 简介](/screenshots/guide/01-intro.png)
```

`.vp-doc` 是 VitePress 正文容器，插件会自动给其中所有图片挂上点击放大。图片资源放 `docs/public/`，引用时用以 `/` 开头的绝对路径（会自动带上 `base`）。

### 4. 验证

- 本地 `npm run docs:dev`，点击任意正文图片：应**立即**出现图片，可缩放/拖拽；
- 点遮罩 / 按 ESC / 点关闭按钮：都应能正常关闭；
- 部署后到线上再点一次确认（GitHub Pages 的 HTML 有约 10 分钟缓存，验证前先 Ctrl+F5 强刷）。

## 六、可选项与避坑清单

- **可选**：自定义查看器样式（遮罩底色、关闭按钮、层级）放在 `theme/style/custom.css`，用 `.viewer-container / .viewer-backdrop / .viewer-close` 选择器，记得加 `!important` 覆盖 viewerjs 默认值。
- **避坑 1**：不要把希望寄托在改 `background-attachment` / 加深遮罩底色等「视觉层」改动上 —— 那治标不治本，真正的根因是 `transition` 依赖 `transitionend`。
- **避坑 2**：`imageViewer` 的签名是 `imageViewer(route, el?, option?)`，`el` 默认 `.vp-doc`。要传第三个参数 `option` 时，第二个参数 `el` 必须显式写出来（如 `'.vp-doc'`），不能省略。
- **避坑 3**：改的是主题入口 `setup()`，dev 下 HMR 有时不生效，必要时重启 `npm run docs:dev`。
- **部署**：本仓库部署分支是 `gh-pages`，`push` 到该分支会触发 GitHub Actions（`.github/workflows/deploy.yml`）自动 `npm run docs:build` 并发布；`dist` 是 gitignore 的，**只需提交源码**。

---

_记录时间：2026-06-16 · 关联提交：`fix(docs): 彻底修复图片查看器灰屏/无图/无法关闭`_
