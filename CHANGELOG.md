# 更新日志

记录 ccMesh 各版本的重要变化。

## [未发布]

### 新增

- 统计页：用量统计与端点统计新增近一年调用热力图（贡献图风格，宽度自适应铺满，悬停查看当日请求数与输入/输出/缓存 Token）
- 统计页：新增调用趋势图（面积图，次数/Tokens 指标切换，跟随所选时间范围按天展示、缺失日期补零）
- 统计页：时间筛选统一升级为日期范围选择器，预设快捷项点击即生效，支持自定义起止日期时间（手写月历，零新增日期依赖）；端点统计的今日/昨日/本周/本月周期转为快捷项，任意自定义区间的指标卡与端点明细由历史数据聚合
- 统计页：端点统计上下区块共用同一时间范围，请求记录支持精确到时分过滤；所选范围无数据时明细表与请求记录整块隐藏
- 用量统计：本机用量记录落库精确时间戳（migration v17），支持时分粒度筛选，升级后首次同步自动回填历史记录
- 配置档案：Claude 配置新增上下文窗口 tokens 与自动压缩触发窗口表单，含推荐值快捷填入
- 配置档案：Claude 新增更多配置弹层，可跳过 Claude Code 初次安装确认（读写 `~/.claude.json`）
- 配置档案：新增 Pi / OMP 配置管理（后端接管 + 配置工作区页面）
- 配置档案：配置文件卡项样式美化
- 端点管理：API URL 路径已含 `/vN`（如智谱 `/v4`）时不再叠一层 `/v1`；末尾加 `#` 可强制不加 `/v1`，表单实时预览完整请求地址
- 工具会话：支持扫描、查看与删除本机 OpenCode 会话（兼容 legacy JSON 与 `opencode.db`），筛选与图标与 Claude / Codex 一致，恢复命令可复制
- 文档：新增 docs-site 文档站点与 GitHub Pages 自动部署工作流

### 修复

- macOS：关于页探测 CLI 版本不再反复触发「下载文件夹」权限请求（探测降级为非交互 shell，不再执行 .zshrc 插件）
- 防止自动更新连点多次下载
- Claude 流式互转：text / thinking 切换时 content block 索引冲突
- 生产构建分包循环初始化导致整窗白屏

## [0.2.3]

### 新增

- 配置档案 / Claude Desktop：接管本机真实配置目录，三栏编辑 Profile、`_meta` 与桌面配置文件；Windows / macOS / Linux 均有路径解析（Windows 含 MSIX 探测，Mac/Linux 按约定 3P 目录，可用环境变量覆盖）
- 配置档案 / Claude Desktop：支持独立开关 3P 模式，打开配置目录，应用配置并标识当前生效项
- 配置档案 / Claude Desktop：支持模型发现与模型别名；开启发现时自动拉取模型，关闭后可配置界面别名
- 对话：新增内置对话页，支持模型选择、消息列表与话题管理
- 工具会话：新增工具会话页面与导航入口
- 桌宠：新增宠物管理与精灵展示，接入侧栏导航与设置入口
- 端点管理：模型映射增加总开关、快捷预设与卡片高亮
- 同步：导出/导入与 WebDAV 同步覆盖模型映射及总开关
- 模型图标：未匹配模型改用通用图标兜底，完善 `codex` 前缀识别

### 修复

- 代理熔断：未知入站路径返回 404 时不再误触发端点熔断

## [0.2.2]

### 新增

- 更新下载：右下角全局下载进度卡，集中管理下载/安装进度状态
- 请求监控新增模型列：单行展示请求模型，映射时同时展示入站模型与实际模型
- WebDAV 同步完整保留端点配置字段，恢复时补齐 `models` / `active_models` / `mappings` 等，并排除 `daily_stats` 统计
- claude code 模型推理强度正常映射 gpt 系列模型推理强度

### 修复

- macOS/Linux 更新安装前先释放代理资源，macOS 安装后改由 LaunchServices 重启
- 三种导航状态下更新提示保持一致，折叠侧栏支持更新角标
- 归档端点不再阻断未归档端点拖拽排序校验
- 请求监控模型列空入站展示与长模型名截断边界
- 清理前端与构建告警

## [0.2.1]

### 新增

- 端点筛选栏：类型标签页、动画搜索框，启用/视图收入设置菜单
- 端点筛选状态下支持拖拽排序
- 请求监控支持手动清理明细
- 快速队列：常用端点优先轮询，仪表盘双队列拖拽管理（最高优先级）
- 端点归档：移入归档、侧栏查看与还原/删除

### 修复

- 各页面状态切换或操作后刷新更及时（含修改代理地址/启停/UA 后仪表盘滞后）
- 端点连通性测试通过后卡片仍显示不可用或熔断中
- 精简代理转发过程日志，降低正常运行时的日志噪音

## [0.2.0]

### 新增

- 同步页 cc-switch 配置迁移：读取本机 `~/.cc-switch/cc-switch.db`，预览识别 claude / codex 供应商，勾选添加
- 端点管理页改为中右布局：列表居中，右侧栏封装端点统计与可用模型
- 移植 `@lobehub/icons` 模型品牌图标（端点卡片、可用模型、表单清单等）
- 请求日志落库端点 transformer 快照（migration v11），旧记录不受后续改类型影响
- 请求监控列表端点列按 transformer 显示品牌图标（旧行回退入站协议）
- 关于页：应用信息、GitHub/发布链接、版本号展示
- 本地环境检查：Claude Code / Codex / OpenCode / Pi 四工具版本探测、安装/升级与冲突诊断
- 设置页卡片布局：General / Proxy / Startup / Advanced 四卡片
- 日志页卡片栈布局：LogRow 卡片化，等级 Badge 与边框语义色
- 仪表盘 ServiceCard 代理网关地址一键复制
- 导航接入「关于」页，VersionPopover 增加关于入口
- Mac 更新流程重构：`install_update_and_restart`（download → install → 停代理 → 清理托盘/单实例锁 → restart）

### 修复

- 仪表盘拖动排序后未及时持久化
- Linux AppImage 交互层问题
- 下载更新后自动重启以载入新版
- 禁用代理层 Axum 默认 2MB body 限制，大上下文请求不再 413
- 模型映射弹窗描述优化；统计页历史记录弹窗表格溢出
- 端点管理页/顶部导航隐藏滚动条；顶栏支持滚轮横向滚动
- 全局 toast 移至右上角；密码框隐藏浏览器原生显隐按钮
- EndpointCard 仅地址可复制（名称不再误触复制）
- Codex 多轮历史切 OpenAI Chat 端点时，`function_call.arguments` 残缺 JSON 导致上游 `prefill failed`：增加完整性校验，合法规范化、残缺降级为 `{}`
- Token 用量 `cache_read` 双重计算：OpenAI 风格 input 归一化为净输入
- 精简代理转发日志：请求热路径降噪，INFO 级别每请求仅保留入站摘要
- 日志页 StrictMode 双订阅导致同条日志显示两次
- 清空日志后切换页面又恢复旧日志（同步清空后端环形缓冲）

## [0.1.7] - 2026-06-22

### 新增

- 请求监控（RequestMonitor）展示上游响应错误详情
- OpenAI Codex 默认 User-Agent 适配与 UA 设置项优化
- Linux 依赖安装脚本

### 修复

- Linux 桌面：WebKitGTK 环境变量规避渲染与输入失效；首屏 show 后窗口重激活；单例/托盘/命令显示路径接线
- 代理熔断：移除 `select_candidates` 全 Open 兜底；forward 空候选守卫；熔断后二次模型过滤，避免误转发不支持该模型的端点
- activeModels 语义：刷新模型时保持 `activeModels` 为空、不自动点亮全部；移除 update 时对 `active_models` 的自动补齐，保持空数组语义
- 全候选端点熔断返回空时，修正为「模型不支持」而非误导性错误
- 精简熔断二次过滤相关日志输出

## [0.1.6] - 2026-06-18

### 新增

- 无独立功能提交

### 修复

- Responses 端点出站兜底规则错误，导致部分路由转发异常

## [0.1.5] - 2026-06-15

### 新增

- 应用单例与桌面快捷唤起（再次启动聚焦已有窗口）
- 端点模型点亮态（`active_models` 子集 + 前端 `activeModels` UI）
- 模型映射出站候选按点亮模型过滤
- 启动行为：静默启动、开机自启、自动运行代理（后端插件 + 设置 UI）
- 端点卡片 API URL 支持 http/https 点击跳转浏览器
- 端点类型筛选增加 Codex
- 配置档案删除渠道确认弹窗文案优化
- 版本更新脚本增强（支持升级类型与说明）

### 修复

- 代理端口统一以 `app_config.port` 为真相源；保存设置后同步刷新配置档案
- Codex 默认 TOML 端口随设置动态更新
- `/v1/models` 跨端点公布模型去重
- 端点模型点亮前后端联动 bug
- 弹窗限高并支持内容滚动；统计历史弹窗加宽
- macOS Dock 图标点击恢复并聚焦主窗口
- macOS 图标边距导致 Dock 图标偏大（重新生成 `.icns`）
- 亮色模式主色按钮文字可读性（绿色底改白字）

---

> 💬 **QQ 反馈交流群：1105537401**
